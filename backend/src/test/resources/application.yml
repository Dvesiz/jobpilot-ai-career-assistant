spring:
  datasource:
    url: jdbc:h2:mem:resumeor-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
app:
  jwt:
    secret: resumeor-test-secret-key-that-is-long-enough-for-hmac-sha256
    expiration-seconds: 3600
  redis:
    enabled: false
